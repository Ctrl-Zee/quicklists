import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indysmittysmith.quicklists',
  appName: 'quicklists',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
